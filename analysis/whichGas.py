""" 
This section is just the sudo code

First will have to parse the values of the diffrence in intesity of returned photons to the spectrometer. Plot this on a graph of the intesity vs a 

This img shows sthe range:  https://meteor.geol.iastate.edu/gccourse/forcing/images/image7.gif
"""
# Imports 
import numpy

# This will be the data
data = np.array([[0.8,0.2...],[1.1, 1.2 ...], np.f32])
# Data uncer
dataUncr = np.array([[0.8,0.2...],[1.1, 1.2 ...], np.f32])
# sep CSV file for matching intesitys across wavelength range for each gas 
# Parse Data (might need a fori trans here dk

# Absob vs Wavlength 2 dim array 

# Cross out normal atmospheric absortation 

def checkCH4(): # Methane
    # Check values of array +- the uncr 
    if valueCer <= 70
        return( True, valueCer) 

def checkN2O(): # Nitrous Oxide 
    # Check values of array +- the uncr 
    if valueCer <= 70
        return( True, valueCer) 


def checkCO2(): # Carbon Dioxide
    # Check values of array +- the uncr 
    if valueCer <= 70
        return( True, valueCer) 






